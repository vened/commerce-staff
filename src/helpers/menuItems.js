import urls from './urls';

const sidebarItems = [
    { name: urls.root.name, url: urls.root.url },
    { name: urls.admins.name, url: urls.admins.url },
    { name: urls.categories.list.name, url: urls.categories.list.url }
];

export default sidebarItems;
